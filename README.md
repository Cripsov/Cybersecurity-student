So, all summer i`ll study for 2-3 hours every day. That is my diary for every day of summer.

Day 1:
Today I read about SQL-injections on PortSwigger and completed 3 labs on Portswigger. Now I know what are SQL-Injections, basic types of injections and what are SELECT, INSERT, UNION, NULL and etc.
Writeups:
        PortSwigger:
            WHERE Clause:
                 You have to change category parameter and inject into the category parameter 'OR 1=1-- and you`ll see unreleased gifts
            SQL-Injection for login: 
                 You have to write in password 'OR 1=1-- and you will login without any password
            Determining the number of collums: 
                 just use NULL
            Lab: SQL injection UNION attack, retrieving multiple values in a single column:
                 You have to get the number of collums and find type of data. Just use NULL, NULL and change NULL on 'a' to check data type
            

